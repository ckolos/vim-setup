---
kind: pipeline
type: docker
name: vim-setup

steps:
- name: test vim
  image: python:slim-buster
  commands:
  - apt update
  - apt-get install -y vim
  - bash -x vimsetup.sh
  - vim -s test_script.vim
