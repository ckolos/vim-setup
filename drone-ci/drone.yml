---
kind: pipeline
type: docker
name: vim-setup

steps:
- name: install vim
  image: python:slim-buster
  commands:
  - apt update
  - apt-get install -y vim
- name: test vim
  commands:
  - bash -x vimsetup.sh
  - vim -s test_script.vim
